<nav class=" navbar navbar-expand-lg navbar-light ws-navbar">
    <div class="container-fluid main-container">
        <div class="element">
            <a href="/">
                <button class="glowing-btn" type="button">
                    <span class="glowing-text">Wild Series</span>
                </button>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent"></div>
        </div>

        <div class="element">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 m-auto">
                <li class="nav-item dropdown">
                    <a class="text-white nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Catégories
                    </a>
                    <ul class=" dropdown-menu" aria-labelledby="navbarDropdown">
                        {% for category in categories.genres %}
                            <li><a class="dropdown-item" href="/category/{{ category }}">{{ category }}</a></li>
                        {% endfor %}
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="#">Something else here</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="text-white nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Acteurs
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="text-white dropdown-item" href="#">Action</a></li>
                        <li><a class="text-whitedropdown-item" href="#">Another action</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="#">Something else here</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="text-white nav-link active" aria-current="page" href="#">Mes Favoris</a>
                </li>
            </ul>
        </div>

        <div class="element">
            <div class="glowing-btn" style="width: 100%; height: 120px">

                {% if app.user is not empty %}
                    <div style="position:relative; height: 100%">
                         {% if app.user is defined and app.user is not empty %}
                            <a href="/user/{{ app.user.id }}" style="font-size: 20px;
                            letter-spacing:1rem;position: absolute;top: 50%; left: 50%;transform: translate(-50% ,
                            -50%); width: 70%">
                                <span>{{ app.user.firstname }} {{ app.user.lastname }}</span>
                            </a>
                         {% endif %}
                    </div>
                {% endif %}
                    <div>
                        <!-- Button trigger modal -->
                        <div style="display: flex">
                            {% if app.user is empty %}

                                <span type="button" data-toggle="modal" data-target="#exampleModalCenter"
                                        class="button-con glowing-text" style="font-size: larger; width: min-content;
                                        letter-spacing: initial; position: absolute; top: 50%; left: 50%;transform: translate(-50% ,-50%); ">
                                    Connexion
                                </span>
                            {% endif %}
                            {% if app.user is not empty %}
                                <div style="position:relative;">
                                    <a href="/logout" style="font-size: medium; width: min-content;letter-spacing:
                                    initial; position: absolute; top: 50%;transform: translate(-50% ,-50%);
                                    padding-bottom: 30px; margin-left: 540px">
                                        Déconnexion
                                    </a>
                                </div>
                            {% endif %}

                        </div>

                    </div>
            </div>
        </div>
        {% include '/includes/_modal-form.html.twig' %}
        {#                    <form class="d-flex"> #}
        {#                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> #}
        {#                        <button class="btn btn-outline-success" type="submit">Search</button> #}
        {#                    </form> #}

    </div>
</nav>